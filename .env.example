# GEMINI_API_KEY: Required for Gemini AI API calls.
GEMINI_API_KEY=""

# Nodemailer configuration
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""

# If you would prefer to use environment variables for Firebase instead of the firebase-applet-config.json file, you could set VITE_ prefixed variables, but the json is perfectly fine for Railway.
